### Fastapi Template

- This template has the `fastapi[all]`
- This template implements CORS middleware by default
- This has the Asgardeo JWKS integration for authentication and authorization
- This also implements connection to MySQL database
- Also basic CRUD operations are also implemented
- Also this is multiple file project
- Dockerfile is also included
